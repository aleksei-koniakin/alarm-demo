package patches.buildTypes

import jetbrains.buildServer.configs.kotlin.*
import jetbrains.buildServer.configs.kotlin.buildFeatures.ParallelTestsFeature
import jetbrains.buildServer.configs.kotlin.buildFeatures.parallelTests
import jetbrains.buildServer.configs.kotlin.ui.*

/*
This patch script was generated by TeamCity on settings change in UI.
To apply the patch, change the buildType with id = 'RunBackendTests'
accordingly, and delete the patch script.
*/
changeBuildType(RelativeId("RunBackendTests")) {
    features {
        val feature1 = find<ParallelTestsFeature> {
            parallelTests {
                numberOfBatches = 3
            }
        }
        feature1.apply {
            numberOfBatches = 4
        }
    }
}
